<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    android:id="@+id/conversationLayout"
    xmlns:android="http://schemas.android.com/apk/res/android" android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/msgContainer"
        android:layout_width="match_parent"
        android:layout_alignParentTop="true"
        android:layout_above="@id/conversationBar"
        android:layout_height="wrap_content" />

    <RelativeLayout
        android:id="@+id/conversationBar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:background="@color/bg_1c"
        android:orientation="horizontal">

        <EditText
            android:id="@+id/message_editText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentLeft="true"
            android:layout_alignParentTop="true"
            android:layout_centerInParent="true"
            android:layout_marginVertical="10dp"
            android:layout_marginLeft="20dp"
            android:layout_toLeftOf="@id/msgSend_btn"
            android:background="@drawable/msg_bubble"
            android:backgroundTint="@color/bg_2c"
            android:hint="Text"
            android:padding="10dp"
            android:textColorHint="@color/txt_2c"
            android:textSize="16sp"

            />

        <ImageButton
            android:id="@+id/msgSend_btn"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_alignParentRight="true"
            android:layout_centerInParent="true"
            android:background="#00000000"
            android:padding="10dp"
            android:scaleType="fitCenter"
            android:src="@drawable/ic_paper_plane_solid" />
    </RelativeLayout>
</RelativeLayout>